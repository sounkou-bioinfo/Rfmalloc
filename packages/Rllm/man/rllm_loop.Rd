% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rllm_program.R
\name{rllm_loop}
\alias{rllm_loop}
\title{Trace a structured, shared recurrence}
\usage{
rllm_loop(x, times, body, name = "loop")
}
\arguments{
\item{x}{A traced value or named list of values from one program.}

\item{times}{A positive integer or one symbolic count.}

\item{body}{A function returning the same value structure as \code{x}.}

\item{name}{Loop name.}
}
\value{
A traced value, or a named list of traced values when \code{x} is a list.
}
\description{
Unlike an R \code{for} loop, \code{rllm_loop()} does not unroll its body. The body is
traced once into a nested data-only program and retained as a loop node.
A single value supports pipe syntax. A named list supports multiple carried
values, including nested recurrences with shared parameters.
}
