% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rllm_model.R
\name{rllm_kv_cache}
\alias{rllm_kv_cache}
\title{Create program-shaped state for incremental decoding}
\usage{
rllm_kv_cache(model, n_ctx = 512L, runtime = NULL)
}
\arguments{
\item{model}{An \code{rllm_model} from \code{\link[=rllm_gguf_model]{rllm_gguf_model()}}.}

\item{n_ctx}{Maximum number of positions the cache can hold.}

\item{runtime}{Optional \code{\link[Rfmalloc:open_fmalloc]{Rfmalloc::open_fmalloc()}} runtime for the slabs.}
}
\value{
An environment of class \code{rllm_kv_cache} with per-layer \code{k}, \code{v},
\code{conv} and \code{recurrent} state, plus \code{n_ctx} and \code{n_past}.
}
\description{
Allocates the persistent state declared by the bound program. Attention
layers receive key/value slabs, short-convolution layers receive causal
history,
and gated-delta layers receive both convolution history and recurrent
matrices. State is plain R memory by default or \pkg{Rfmalloc}-backed when
\code{runtime} is supplied.
}
\details{
The returned object is an environment, so \code{\link[=rllm_forward]{rllm_forward()}} can advance its
\code{n_past} by reference.
}
\seealso{
\code{\link[=rllm_forward]{rllm_forward()}}, \code{\link[=rllm_generate]{rllm_generate()}}
}
