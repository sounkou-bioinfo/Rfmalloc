% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rllm_generate.R
\name{rllm_decode}
\alias{rllm_decode}
\alias{rllm_encode}
\title{Convert between token ids and raw bytes}
\usage{
rllm_decode(model, ids)

rllm_encode(model, x)
}
\arguments{
\item{model}{An \code{rllm_model} whose GGUF carries a byte-level BPE tokenizer.}

\item{ids}{Integer vector of 0-based token ids.}

\item{x}{A raw vector (or a single string, converted with \code{charToRaw()}).}
}
\value{
\code{rllm_decode()}: a raw vector. \code{rllm_encode()}: an integer vector
of 0-based token ids.
}
\description{
The ids <-> bytes edge codecs of the bytes-boundary API, built from the
model's own GGUF tokenizer metadata (byte-level BPE, \code{tokenizer.ggml.model == "gpt2"}). \code{rllm_decode()} maps ids to the exact bytes they stand for.
\code{rllm_encode()} byte-pair-encodes bytes into ids using the file's merge
ranks; it applies the merges without GPT-2's regex pre-tokenizer, so a
tokenization may occasionally differ from llama.cpp's canonical split -
every output is still a valid encoding of the input
(\code{rllm_decode(rllm_encode(x))} is always \code{x}).
}
