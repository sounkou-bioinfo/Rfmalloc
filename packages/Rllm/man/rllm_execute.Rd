% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rllm_program.R
\name{rllm_execute}
\alias{rllm_execute}
\title{Execute a data-only architecture program}
\usage{
rllm_execute(
  program,
  inputs,
  parameters = list(),
  counts = list(),
  operators = list()
)
}
\arguments{
\item{program}{A data-only \code{rllm_program}.}

\item{inputs}{A named list of input values. Extra entries remain available
to operator functions through \code{context$inputs}.}

\item{parameters}{A named list containing a value for every declared
program parameter.}

\item{counts}{A named list or atomic vector resolving symbolic loop counts
and other symbolic integer attributes.}

\item{operators}{A named list of semantic operator functions.}
}
\value{
A named list containing every program output and tap.
}
\description{
\code{rllm_execute()} interprets the dataflow and structured loops in an
\code{\link[=rllm_program]{rllm_program()}}. Semantic operators are ordinary R functions supplied in
\code{operators}; a small dense reference vocabulary is provided for arithmetic
shared by the architecture probes. This is a reference and lowering
harness, not the GGUF execution path used by \code{\link[=rllm_forward]{rllm_forward()}}.
}
\details{
Each operator function is called with four named arguments: \code{inputs}, the
list of values arriving at the node; \code{attributes}, the node's data-only
attributes; \code{parameters}, the named parameter values; and \code{context}, a
list containing the current node and program, root and local inputs,
symbolic counts, and the enclosing loop iterations. Operator functions
control the representation of their results, so a lowering may preserve a
mapped or device-backed value rather than materializing it in R.

The built-in dense reference operators are \code{add}, \code{linear}, \code{rms_norm},
\code{layer_norm}, \code{pool}, \code{gelu}, \code{silu}, \code{sigmoid}, \code{tanh},
\code{broadcast_initial}, \code{select_first_token}, \code{drop_puzzle_prefix} and
\code{swiglu}. Entries in \code{operators} replace built-ins with the same name.
}
