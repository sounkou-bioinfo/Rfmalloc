% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rllm_program.R
\name{rllm_linear}
\alias{rllm_linear}
\alias{rllm_norm}
\alias{rllm_attention}
\alias{rllm_pool}
\title{Typed linear, normalization, attention and pooling operators}
\usage{
rllm_linear(x, weight, bias = NULL)

rllm_norm(x, weight, kind = c("rms", "layer"), eps = 1e-05, bias = NULL)

rllm_attention(
  x,
  query,
  key,
  value,
  output,
  heads,
  kv_heads = heads,
  query_norm = NULL,
  key_norm = NULL,
  rope = NULL,
  mask = list(type = "causal"),
  scale = NULL
)

rllm_pool(x, kind = c("mean", "cls", "none"))
}
\arguments{
\item{x}{A traced \code{rllm_value}.}

\item{weight, bias}{Tensor references from \code{\link[=rllm_parameter]{rllm_parameter()}}. \code{bias} is
optional.}

\item{kind}{Operator kind.}

\item{eps}{Normalization epsilon.}

\item{query, key, value, output}{Projection parameters for attention.}

\item{heads, kv_heads}{Query and key/value head counts.}

\item{query_norm, key_norm}{Optional per-head normalization parameters.}

\item{rope, mask, scale}{Data-only attention specifications.}
}
\value{
A traced \code{rllm_value}.
}
\description{
These are convenient typed constructors over \code{\link[=rllm_op]{rllm_op()}}. They keep the
surface close to an R module's \code{forward} method while recording explicit
parameter identities and semantic attributes in the frozen program.
}
