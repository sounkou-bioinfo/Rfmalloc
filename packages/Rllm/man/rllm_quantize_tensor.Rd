% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/quantize.R
\name{rllm_quantize_tensor}
\alias{rllm_quantize_tensor}
\title{Quantize a matrix into an Rfmalloc-backed quantized tensor}
\usage{
rllm_quantize_tensor(x, dtype = "q4_k", runtime = NULL)
}
\arguments{
\item{x}{A numeric matrix to quantize.}

\item{dtype}{Target quantized codec, one of \code{"q4_0"}, \code{"q4_1"},
\code{"q8_0"}, \code{"q2_k"}, \code{"q4_k"} (default), \code{"q6_k"}.}

\item{runtime}{Optional \pkg{Rfmalloc} runtime handle
(see \code{\link[Rfmalloc:open_fmalloc]{Rfmalloc::open_fmalloc()}}); if \code{NULL}, Rfmalloc's default
runtime is used.}
}
\value{
An \code{fmalloc_tensor} of the given \code{dtype} with
\code{dim(x)}.
}
\description{
Encodes a dense numeric matrix into a GGUF quantized block format and stores
the compressed payload in \pkg{Rfmalloc}-backed (file-backed, memory-mapped)
storage, returning an \code{fmalloc_tensor}. This is the write-side
counterpart to \pkg{Rgguf}'s \code{gguf_tensor(..., as = "native")}: the
resulting tensor keeps its quantized storage density and, when multiplied as
the right-hand operand of \code{dense \%*\% tensor} with the ggml backend
active (see \code{\link[=rllm_use_ggml]{rllm_use_ggml()}}), is contracted natively in quantized space by
GGML's SIMD-dispatched kernels without ever being decoded to double.
}
\details{
The number of rows of \code{x} is the quantized (per-row) dimension and must
be a multiple of the codec's block size: 256 for the K-quants
(\code{"q2_k"}, \code{"q4_k"}, \code{"q6_k"}) and 32 for \code{"q4_0"},
\code{"q4_1"}, \code{"q8_0"}.
}
\examples{
rt <- Rfmalloc::open_fmalloc(tempfile(fileext = ".bin"))
set.seed(1)
W <- matrix(rnorm(256 * 4, sd = 0.4), nrow = 256)  # 256 must divide nrow
Wt <- rllm_quantize_tensor(W, "q4_k", runtime = rt)
X <- matrix(rnorm(3 * 256), nrow = 3)              # 3 x 256
Y <- X \%*\% Wt                                      # native quantized product
dim(Y)
}
\seealso{
\code{\link[=rllm_use_ggml]{rllm_use_ggml()}}, \code{\link[Rfmalloc:fmalloc_tensor]{Rfmalloc::create_fmalloc_tensor()}}
}
