% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rpgen_import.R
\name{rpgen_import_haps}
\alias{rpgen_import_haps}
\title{Convert Oxford-format phased haplotypes (.haps/.legend/.sample) to a .pgen}
\usage{
rpgen_import_haps(haps, legend, sample, chr, out = tempfile(fileext = ".pgen"))
}
\arguments{
\item{haps}{Path to a \code{.haps} file.}

\item{legend}{Path to the companion \code{.legend} file.}

\item{sample}{Path to the companion \code{.sample} file.}

\item{chr}{Chromosome code for every variant in \code{legend} (e.g. \code{"1"}).
Required; see this function's Details.}

\item{out}{Path to the \code{.pgen} to produce; must end in \code{.pgen}. Defaults
to a fresh \code{\link[=tempfile]{tempfile()}}.}
}
\value{
\code{out}, invisibly on success; an R error is raised on failure.
}
\description{
Converts the Oxford-format \code{haps}/\code{legend}/\code{sample} triple to a \code{.pgen} by
calling plink2's own \code{OxHapslegendToPgen()} importer, with the defaults a
plain \verb{plink2 --haps <haps> --legend <legend> <chr> --sample <sample> --make-pgen} would use.
}
\details{
A \code{.haps}/\code{.legend} pair encodes \emph{phased} haplotypes, and the produced
\code{.pgen} faithfully carries that phase - but Rpgen's current readers
(\code{\link[=rpgen_read_hardcalls]{rpgen_read_hardcalls()}}, \code{\link[=rpgen_read_dosages]{rpgen_read_dosages()}}) only read the
collapsed, unphased hardcall/dosage view; reading phase back is a future
milestone. The genotypes themselves (0/1/2 copies of the alternate
allele) still round-trip correctly through the existing readers - only
which haplotype carries which allele is not yet recoverable.

\code{chr} is required: the classic IMPUTE2 \code{.legend} format (\verb{id position a0 a1}, no chromosome column) does not carry its own chromosome, so plink2
itself requires one whenever \code{--legend} is used (confirmed via
\verb{plink2 --help legend}).
}
\seealso{
\code{\link[=rpgen_import_gen]{rpgen_import_gen()}}, \code{\link[=rpgen_import_bgen]{rpgen_import_bgen()}}, \code{\link[=rpgen_read_hardcalls]{rpgen_read_hardcalls()}}
}
