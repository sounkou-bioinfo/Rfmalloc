% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rpgen_import.R
\name{rpgen_import_bgen}
\alias{rpgen_import_bgen}
\title{Convert a BGEN file to a .pgen using plink2's own importer}
\usage{
rpgen_import_bgen(bgen, sample = NULL, out = tempfile(fileext = ".pgen"))
}
\arguments{
\item{bgen}{Path to a BGEN file (v1.1, v1.2, or v1.3).}

\item{sample}{Path to a companion \code{.sample} file, or \code{NULL} (the default)
to use the BGEN's own embedded sample identifiers, if present.}

\item{out}{Path to the \code{.pgen} to produce; must end in \code{.pgen}. Defaults
to a fresh \code{\link[=tempfile]{tempfile()}}.}
}
\value{
\code{out}, invisibly on success; an R error is raised on failure.
}
\description{
Converts \code{bgen} (any of v1.1/v1.2/v1.3) to a \code{.pgen} by calling plink2's
own \code{OxBgenToPgen()} importer, with the defaults a plain
\verb{plink2 --bgen <bgen> --sample <sample> --make-pgen} (or, if \code{sample} is
\code{NULL}, \verb{plink2 --bgen <bgen> --make-pgen} alone) would use. Unlike
\code{\link[=rpgen_import_gen]{rpgen_import_gen()}}'s \code{.gen}, a BGEN v1.2/v1.3 file may carry its own
sample identifier block, so \code{sample} may be omitted when the file has one
\itemize{
\item plink2's own importer raises a clear error if it does not.
}
}
\details{
The produced \code{.pgen} can be read back with any of Rpgen's existing \code{.pgen}
readers, exactly as \code{\link[=rpgen_import_vcf]{rpgen_import_vcf()}}'s can.
}
\examples{
bgen <- system.file("extdata", "tiny.bgen", package = "Rpgen")
samp <- system.file("extdata", "tiny.sample", package = "Rpgen")
if (nzchar(bgen) && nzchar(samp)) {
    pgen <- rpgen_import_bgen(bgen, sample = samp)
    rpgen_info(pgen)$n_sample
    unlink(c(pgen, sub("\\\\.pgen$", ".pvar", pgen), sub("\\\\.pgen$", ".psam", pgen)))
}
}
\seealso{
\code{\link[=rpgen_import_gen]{rpgen_import_gen()}}, \code{\link[=rpgen_import_haps]{rpgen_import_haps()}}
}
