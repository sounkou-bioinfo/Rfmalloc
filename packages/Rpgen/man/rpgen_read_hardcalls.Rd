% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rpgen.R
\name{rpgen_read_hardcalls}
\alias{rpgen_read_hardcalls}
\alias{rpgen_read_dosages}
\title{Read genotypes from a .pgen file as a dense R matrix}
\usage{
rpgen_read_hardcalls(path, pvar = NULL)

rpgen_read_dosages(path, pvar = NULL)
}
\arguments{
\item{path}{Path to a \code{.pgen} file.}

\item{pvar}{Path to the companion \code{.pvar}/\code{.pvar.zst} file. Accepted for
API symmetry with a future allele-specific reader; not read yet (see
Details) - the collapsed-ALT encoding these functions produce does not
need it. Defaults to \code{NULL}.}
}
\value{
\code{rpgen_read_hardcalls()} returns an integer matrix of \code{0}, \code{1},
\code{2}, or \code{NA} hardcall dosages. \code{rpgen_read_dosages()} returns a numeric
matrix of dosages in \verb{[0, 2]}, or \code{NA} for missing.
}
\description{
Reads every sample and every variant from a PLINK 2 \code{.pgen} file through
Rpgen's vendored pgenlib, via \code{plink2::PgrGet()} (\code{rpgen_read_hardcalls()})
or \code{plink2::PgrGetD()} (\code{rpgen_read_dosages()}). Both return a dense
\verb{n_sample x n_variant} matrix, samples in rows and variants in columns,
matching PLINK's own \code{.bed} orientation. These are thin wrappers around the
\code{RC_rpgen_read_hardcalls}/\code{RC_rpgen_read_dosages} \code{.Call} entry points,
themselves thin wrappers around the \code{Rpgen_read_hardcalls}/
\code{Rpgen_read_dosages} C-callables (see \code{inst/include/Rpgen.h}) - the
lower-level counterpart to \code{\link[=rpgen_bed]{rpgen_bed()}}/\code{\link[=rpgen_dosage]{rpgen_dosage()}}, for callers who
want the plain R matrix rather than an \code{Rfmalloc} tensor.
}
\details{
For a multiallelic variant, \code{plink2::PgrGet()}/\code{PgrGetD()} collapse every
ALT allele into a single non-reference count - the same encoding
\code{pgenlibr::ReadIntList()}/\code{ReadList()} return by calling the identical
pgenlib entry points. Allele identity (which ALT allele is present) is not
needed to produce that collapsed encoding, only for an allele-specific
read (\code{plink2::PgrGet1()}/\code{PgrGet1D()}, not exposed here), which is the
only thing pgenlibr's own \code{.pvar} requirement (\code{NewPgen(..., pvar = )}) at
this fixture's multiallelic-plus-dosage combination actually guards
against - so unlike \code{pgenlibr::NewPgen()}, \code{rpgen_read_hardcalls()}/
\code{rpgen_read_dosages()} do not require a \code{.pvar} for this file at all.
}
\examples{
pgen <- system.file("extdata", "chr21_phase3_start.pgen", package = "Rpgen")
hc <- rpgen_read_hardcalls(pgen)
dim(hc)
ds <- rpgen_read_dosages(pgen)
dim(ds)
}
\seealso{
\code{\link[=rpgen_bed]{rpgen_bed()}}, \code{\link[=rpgen_dosage]{rpgen_dosage()}}
}
