% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rpgen_import.R
\name{rpgen_import_vcf}
\alias{rpgen_import_vcf}
\title{Convert a VCF to a .pgen using plink2's own importer}
\usage{
rpgen_import_vcf(vcf, out = tempfile(fileext = ".pgen"))
}
\arguments{
\item{vcf}{Path to a VCF file (may be gzip/bgzip-compressed; plink2's own
importer detects that from the file's magic bytes, not its extension).}

\item{out}{Path to the \code{.pgen} to produce; must end in \code{.pgen}. Defaults
to a fresh \code{\link[=tempfile]{tempfile()}}. The companion \code{.pvar}/\code{.psam} are written next
to it, with the same base name and their own conventional extensions.}
}
\value{
\code{out}, invisibly on success (matching the input, since plink2's
importer writes exactly there); an R error is raised on failure, with
plink2's own diagnostic already relayed to the R console (see
\code{src/rpgen_import.cpp}'s top comment for why the error path has two
halves).
}
\description{
Converts \code{vcf} to a \code{.pgen} by calling plink2's own \code{VcfToPgen()} importer
(vendored by \verb{tools/vendor-plink2-import/}, see its \code{PROVENANCE.md}) with
the defaults a plain \verb{plink2 --vcf <vcf> --make-pgen} (no other flags)
would use - no argv parsing happens; every default matches what plink2.cc
itself computes for that combination (see \code{src/rpgen_import.cpp}'s
comments for exactly which). The design choice this embodies: reuse
plink2's own import code rather than maintain a second parser for each
format. The identical closure covers BCF, BGEN, and Oxford inputs.
}
\details{
The produced \code{.pgen} (plus its companion \code{.pvar}/\code{.psam}, written next to
it with the same base name) can be read back with any of Rpgen's existing
\code{.pgen} readers - \code{\link[=rpgen_info]{rpgen_info()}}, \code{\link[=rpgen_read_hardcalls]{rpgen_read_hardcalls()}},
\code{\link[=rpgen_read_dosages]{rpgen_read_dosages()}}, or \code{\link[=rpgen_bed]{rpgen_bed()}}/\code{\link[=rpgen_dosage]{rpgen_dosage()}} for the
\code{Rfmalloc} tensor surface - since it is an ordinary \code{.pgen}, not a special
format of its own.
}
\examples{
vcf <- system.file("extdata", "tiny.vcf", package = "Rpgen")
if (nzchar(vcf)) {
    pgen <- rpgen_import_vcf(vcf)
    info <- rpgen_info(pgen)
    info$n_sample
    unlink(c(pgen, sub("\\\\.pgen$", ".pvar", pgen), sub("\\\\.pgen$", ".psam", pgen)))
}
}
\seealso{
\code{\link[=rpgen_bed]{rpgen_bed()}}, \code{\link[=rpgen_read_hardcalls]{rpgen_read_hardcalls()}}
}
