% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rpgen_import.R
\name{rpgen_import_bed}
\alias{rpgen_import_bed}
\title{Convert a VCF straight into an Rfmalloc bed tensor}
\usage{
rpgen_import_bed(
  vcf,
  out = tempfile(fileext = ".pgen"),
  keep = FALSE,
  runtime = NULL
)
}
\arguments{
\item{vcf}{Path to a VCF file; see \code{\link[=rpgen_import_vcf]{rpgen_import_vcf()}}.}

\item{out}{Path to the intermediate \code{.pgen}; see \code{\link[=rpgen_import_vcf]{rpgen_import_vcf()}}.
Defaults to a fresh \code{\link[=tempfile]{tempfile()}}.}

\item{keep}{Keep the intermediate \code{.pgen}/\code{.pvar}/\code{.psam} files instead of
deleting them after the read. Defaults to \code{FALSE}.}

\item{runtime}{Runtime handle from \code{\link[Rfmalloc:open_fmalloc]{Rfmalloc::open_fmalloc()}}; see
\code{\link[=rpgen_bed]{rpgen_bed()}}. Defaults to the runtime established by
\code{\link[Rfmalloc:init_fmalloc]{Rfmalloc::init_fmalloc()}}.}
}
\value{
An \code{fmalloc_tensor} of dtype \code{"bed"}, \verb{n_sample x n_variant}; see
\code{\link[=rpgen_bed]{rpgen_bed()}}.
}
\description{
Convenience wrapper chaining \code{\link[=rpgen_import_vcf]{rpgen_import_vcf()}} and \code{\link[=rpgen_bed]{rpgen_bed()}}: the
VCF is imported to a (by default temporary) \code{.pgen} via plink2's own
importer, immediately read back through Rpgen's existing \code{.pgen} reader,
and packed into fmalloc-backed 2-bit \code{.bed} storage. The intermediate
\code{.pgen}/\code{.pvar}/\code{.psam} files are removed afterward unless \code{keep = TRUE}.
}
\examples{
vcf <- system.file("extdata", "tiny.vcf", package = "Rpgen")
if (nzchar(vcf)) {
    rt <- Rfmalloc::open_fmalloc(tempfile(), size_gb = 0.5)
    tn <- rpgen_import_bed(vcf, runtime = rt)
    dim(tn)
    Rfmalloc::cleanup_fmalloc(rt)
}
}
\seealso{
\code{\link[=rpgen_import_vcf]{rpgen_import_vcf()}}, \code{\link[=rpgen_bed]{rpgen_bed()}}
}
