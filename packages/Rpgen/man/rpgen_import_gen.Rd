% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rpgen_import.R
\name{rpgen_import_gen}
\alias{rpgen_import_gen}
\title{Convert an Oxford-format .gen + .sample to a .pgen using plink2's own importer}
\usage{
rpgen_import_gen(gen, sample, out = tempfile(fileext = ".pgen"))
}
\arguments{
\item{gen}{Path to an Oxford-format \code{.gen} file (original 5-column
layout, with a leading chromosome column).}

\item{sample}{Path to the companion \code{.sample} file. Required.}

\item{out}{Path to the \code{.pgen} to produce; must end in \code{.pgen}. Defaults
to a fresh \code{\link[=tempfile]{tempfile()}}.}
}
\value{
\code{out}, invisibly on success; an R error is raised on failure.
}
\description{
Converts the Oxford-format \code{gen}/\code{sample} pair to a \code{.pgen} by calling
plink2's own \code{OxGenToPgen()} importer, with the defaults a plain
\verb{plink2 --gen <gen> --sample <sample> --make-pgen} would use. Unlike
\code{\link[=rpgen_import_vcf]{rpgen_import_vcf()}}'s VCF/BCF, a \code{.gen} file carries no sample IDs or
pedigree of its own, so \code{sample} is required, not optional.
}
\details{
\code{gen}'s rows must carry an explicit leading chromosome column (plink2's
original 5-column \code{.gen} layout: \verb{chr id pos a1 a2 <probabilities>...});
this function does not expose an \code{--oxford-single-chr} equivalent.

The produced \code{.pgen} can be read back with any of Rpgen's existing \code{.pgen}
readers, exactly as \code{\link[=rpgen_import_vcf]{rpgen_import_vcf()}}'s can.
}
\seealso{
\code{\link[=rpgen_import_bgen]{rpgen_import_bgen()}}, \code{\link[=rpgen_import_haps]{rpgen_import_haps()}}
}
