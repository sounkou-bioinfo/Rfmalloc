% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rpgen.R
\name{rpgen_read_bed_hardcalls}
\alias{rpgen_read_bed_hardcalls}
\title{Read genotypes from a PLINK 1 .bed fileset as a dense R matrix}
\usage{
rpgen_read_bed_hardcalls(bed, bim = NULL, fam = NULL)
}
\arguments{
\item{bed}{Path to a \code{.bed} file.}

\item{bim}{Path to the companion \code{.bim} file. Defaults to \code{bed} with its
extension swapped for \code{.bim}.}

\item{fam}{Path to the companion \code{.fam} file. Defaults to \code{bed} with its
extension swapped for \code{.fam}.}
}
\value{
An integer matrix of \code{0}, \code{1}, \code{2}, or \code{NA} hardcall dosages,
\verb{n_sample x n_variant}, samples in rows and variants in columns.
}
\description{
Reads every sample and every variant from a PLINK 1 \code{.bed}/\code{.bim}/\code{.fam}
fileset through Rpgen's vendored pgenlib. \code{PgfiInitPhase1()} opens a \code{.bed}
transparently, in the exact same code path a \code{.pgen} takes (its \code{vrtypes}
simply come back \code{NULL}) - the one real difference is that a \code{.bed} has no
header to read its sample/variant counts back from, so they are counted
from the companion \code{.fam}/\code{.bim} first (see \code{\link[=rpgen_bed_info]{rpgen_bed_info()}}) and passed
in explicitly. Genotypes are then read via the same \code{plink2::PgrGet()}
\code{\link[=rpgen_read_hardcalls]{rpgen_read_hardcalls()}} uses; a \code{.bed} is biallelic hardcalls only, so
there is no dosage counterpart to this function. This is a thin wrapper
around the \code{RC_rpgen_read_bed_hardcalls} \code{.Call} entry point, itself a
thin wrapper around the \code{Rpgen_read_bed_hardcalls} C-callable (see
\code{inst/include/Rpgen.h}) - the lower-level counterpart to \code{\link[=rpgen_bed]{rpgen_bed()}} for
callers who want the plain R matrix rather than an \code{Rfmalloc} tensor.
}
\seealso{
\code{\link[=rpgen_bed]{rpgen_bed()}}, \code{\link[=rpgen_bed_info]{rpgen_bed_info()}}, \code{\link[=rpgen_read_hardcalls]{rpgen_read_hardcalls()}}
}
