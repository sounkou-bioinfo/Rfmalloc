% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rpgen.R
\name{rpgen_info}
\alias{rpgen_info}
\title{Open a .pgen file and report its sample and variant counts}
\usage{
rpgen_info(path)
}
\arguments{
\item{path}{Path to a \code{.pgen} file.}
}
\value{
A list with \code{n_sample} and \code{n_variant}, both integers.
}
\description{
Opens a PLINK 2 \code{.pgen} file through Rpgen's vendored pgenlib
(\code{PgfiInitPhase1()} / \code{PgfiInitPhase2()} / \code{PgrInit()}), reads its header
counts, and closes it again. This is a thin wrapper around the
\code{RC_rpgen_info} \code{.Call} entry point, itself a thin wrapper around the
\code{Rpgen_open_info} C-callable registered for other packages to link
against (see \code{inst/include/Rpgen.h}).
}
\examples{
pgen <- system.file("extdata", "chr21_phase3_start.pgen", package = "Rpgen")
rpgen_info(pgen)
}
