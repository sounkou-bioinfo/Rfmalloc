% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rpgen_import.R
\name{rpgen_import_plink1_dosage}
\alias{rpgen_import_plink1_dosage}
\title{Convert a legacy PLINK 1 --import-dosage file to a .pgen}
\usage{
rpgen_import_plink1_dosage(dosage, fam, map, out = tempfile(fileext = ".pgen"))
}
\arguments{
\item{dosage}{Path to a PLINK 1 \code{--import-dosage}-format text file (a
header line of space-separated \verb{FID IID} pairs, then one row per
variant of \verb{ID A1 A2 <per-sample value(s)>}).}

\item{fam}{Path to the companion \code{.fam} file.}

\item{map}{Path to the companion \code{.map} file.}

\item{out}{Path to the \code{.pgen} to produce; must end in \code{.pgen}. Defaults
to a fresh \code{\link[=tempfile]{tempfile()}}.}
}
\value{
\code{out}, invisibly on success; an R error is raised on failure.
}
\description{
Converts the legacy PLINK 1.x \code{--import-dosage} text format (\code{dosage},
plus companion \code{fam}/\code{map} files) to a \code{.pgen} by calling plink2's own
\code{Plink1DosageToPgen()} importer, with the defaults a plain
\verb{plink2 --import-dosage <dosage> --fam <fam> --map <map> --make-pgen} (no
\code{--import-dosage} modifiers) would use - in particular, the per-sample
column format (a single dosage value, or a double/triple-probability
layout) is auto-inferred from \code{dosage}'s own column count, the same as
the plain command would do.
}
\details{
The produced \code{.pgen} can be read back with any of Rpgen's existing \code{.pgen}
readers, exactly as \code{\link[=rpgen_import_vcf]{rpgen_import_vcf()}}'s can.
}
\seealso{
\code{\link[=rpgen_import_vcf]{rpgen_import_vcf()}}, \code{\link[=rpgen_read_dosages]{rpgen_read_dosages()}}
}
