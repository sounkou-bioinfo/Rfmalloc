% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rggml_cuda.R
\name{rggml_cuda_info}
\alias{rggml_cuda_info}
\alias{rggml_has_cuda}
\title{CUDA GPU backend availability}
\usage{
rggml_cuda_info()

rggml_has_cuda()
}
\value{
A list with \code{n_devices} (integer) and \code{device} (the description of
device 0, or \code{NA} when there is none).

\code{rggml_has_cuda()} returns \code{TRUE} when at least one CUDA device is
usable.
}
\description{
Reports the NVIDIA CUDA devices GGML can see. The backend is opt-in at
installation because it requires the CUDA toolkit and compiles native GPU
kernels:
}
\details{
\preformatted{
  install.packages("Rggml", configure.args = "--with-cuda")
  R CMD INSTALL --configure-args=--with-cuda .
}

Set \code{CUDA_HOME} when the toolkit is outside the search path. By default the
source installation targets the GPU visible at build time. Set
\code{RGGML_CUDA_ARCH} to an explicit nvcc architecture such as \code{sm_89} or
\code{sm_120a} when building for another machine.

A build without CUDA returns zero devices rather than failing, so callers
can probe and fall back to Vulkan, BLAS or CPU computation.
}
\examples{
rggml_cuda_info()
}
