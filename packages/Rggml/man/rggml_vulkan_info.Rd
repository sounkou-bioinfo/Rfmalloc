% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rggml_vulkan.R
\name{rggml_vulkan_info}
\alias{rggml_vulkan_info}
\alias{rggml_has_vulkan}
\title{Vulkan GPU backend availability}
\usage{
rggml_vulkan_info()

rggml_has_vulkan()
}
\value{
A list with \code{n_devices} (integer) and \code{device} (the description of
device 0, or \code{NA} when there is none).

\code{rggml_has_vulkan()} returns \code{TRUE} when at least one Vulkan device
is usable.
}
\description{
Reports the Vulkan devices GGML can see. Rggml only contains the Vulkan
backend when it was \strong{built with it} - it is opt-in, because generating and
compiling GGML's 156 embedded SPIR-V shaders is expensive (the largest one
needs several GB of RAM):
}
\details{
\preformatted{
  install.packages("Rggml", configure.args = "--with-vulkan")
  R CMD INSTALL --configure-args=--with-vulkan .
}

It also requires \code{glslc} and the Vulkan headers at build time
(\code{libvulkan-dev} + \code{glslc} on Debian/Ubuntu, or the LunarG Vulkan SDK with
\code{VULKAN_SDK} set), and a Vulkan driver at run time. A software driver such
as Mesa's lavapipe counts: it is slow, but it makes the backend testable
without a GPU.

When Rggml was built without Vulkan, this returns zero devices rather than
failing, so callers can probe and fall back.

Two opt-in environment variables widen which devices are usable, both off by
default (so the default is upstream GGML's): \code{GGML_VK_ALLOW_CPU=1} accepts a
CPU-type Vulkan device (e.g. Mesa lavapipe), and \code{GGML_VK_ALLOW_128_PUSH=1}
accepts a device that exposes only 128-byte push constants (e.g. a GPU reached
through the Mesa dzn D3D12 translation layer under WSL) for matrix multiply
and other \verb{<=4D} operations; 5-D non-contiguous copies still require a 256-byte
device.
}
\examples{
rggml_vulkan_info()
}
