% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rggml_test_mul_mat.R
\name{rggml_test_mul_mat_q4k}
\alias{rggml_test_mul_mat_q4k}
\title{(internal) Run a quantized (Q4_K) 'GGML' matmul through the C-callables}
\usage{
rggml_test_mul_mat_q4k(A, B, backend = c("cpu", "blas", "vulkan", "cuda"))
}
\arguments{
\item{A}{Numeric weight matrix; \code{nrow(A)} (the contracted dimension)
must be a multiple of 256 (\code{QK_K}).}

\item{B}{Numeric activation matrix with \code{nrow(B) == nrow(A)}.}

\item{backend}{The GGML backend that owns the tensor buffer.}
}
\value{
A numeric matrix, dim \code{c(ncol(A), ncol(B))}, equal to
\code{crossprod(A, B)} up to q4_K/q8_K quantization error.
}
\description{
Not exported; the quantized analogue of \code{\link[=rggml_test_mul_mat]{rggml_test_mul_mat()}} and the
exact operation the Rfmalloc typed-GEMM bridge performs. The weight matrix
\code{A} is quantized to \code{Q4_K} into a heap buffer standing in for an
mmap'd 'GGUF' payload, wrapped zero-copy as a \code{Q4_K} tensor, and
multiplied by the dense F32 activations \code{B}. \code{ggml_mul_mat()}
contracts each \code{Q4_K} weight row against \code{B}'s columns through
the runtime-SIMD-dispatched \code{ggml_vec_dot_q4_K_q8_K} (AVX2/NEON where
staged), quantizing \code{B} to \code{Q8_K} on the fly as at inference.
}
\keyword{internal}
