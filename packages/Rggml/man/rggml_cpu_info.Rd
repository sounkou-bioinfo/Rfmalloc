% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rggml_cpu.R
\name{rggml_cpu_info}
\alias{rggml_cpu_info}
\title{What this Rggml was actually built with}
\usage{
rggml_cpu_info()
}
\value{
A list with
\describe{
\item{\code{arch_kernels}}{\code{"arm"} for GGML's hand-tuned NEON kernels,
\code{"wasm"} for its SIMD128 kernels, and \code{"generic"} for the portable
reference kernels.}
\item{\code{simd_dispatch}}{\code{TRUE} when the runtime CPUID dispatcher is active
(x86: the staged AVX2 \code{q4_K} variant). Always \code{FALSE} alongside
\code{arch_kernels = "arm"} or \code{"wasm"}, which supersede it.}
\item{\code{blas}}{\code{TRUE} when GGML's BLAS backend and R's Fortran bridge
are part of this target build. It is \code{FALSE} on wasm, where webR's
hidden character-length ABI is incompatible with the native bridge.}
\item{\code{sgemm}}{\code{TRUE} when R's BLAS exports \code{sgemm_} and GGML's BLAS
backend calls it directly; \code{FALSE} when the shim promotes to \code{dgemm_}
or when \code{blas} is \code{FALSE}.}
\item{\code{vulkan}}{\code{TRUE} when built with \code{--with-vulkan}. Whether a \emph{device}
is visible is a separate question: see \code{\link[=rggml_vulkan_info]{rggml_vulkan_info()}}.}
\item{\code{cuda}}{\code{TRUE} when built with \code{--with-cuda}. Whether a \emph{device}
is visible is a separate question: see \code{\link[=rggml_cuda_info]{rggml_cuda_info()}}.}
}
}
\description{
Reports the build decisions \code{configure} made, read off the preprocessor
symbols compiled into the shared object. This is deliberately not a
re-detection of the host: it answers "what is in this binary", not "what
could this machine run".
}
\details{
It exists because the failure mode it guards against is invisible. A build
that misses its intended branch - falling back to GGML's portable scalar
kernels where the native NEON ones were meant, or to the \code{dgemm_} promotion
where \code{sgemm_} was available - compiles cleanly and passes every numerical
test, because both branches are correct. Only the speed differs. Asserting on
this list in the test suite turns those silent fallbacks into failures.
}
\examples{
rggml_cpu_info()
}
\seealso{
\code{\link[=rggml_vulkan_info]{rggml_vulkan_info()}}, \code{\link[=rggml_cuda_info]{rggml_cuda_info()}}
}
