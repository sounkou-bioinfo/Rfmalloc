% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/statgen_ldpred2.R
\name{statgen_ldpred2}
\alias{statgen_ldpred2}
\alias{statgen_ldpred2_inf}
\alias{statgen_ldpred2_auto}
\title{LDpred2: Bayesian polygenic scores over a banded LD matrix}
\usage{
statgen_ldpred2_inf(corr, df_beta, h2)

statgen_ldpred2(
  corr,
  df_beta,
  h2,
  p,
  sparse = FALSE,
  burn_in = 50L,
  num_iter = 100L
)

statgen_ldpred2_auto(
  corr,
  df_beta,
  h2_init,
  p_init = 0.1,
  burn_in = 500L,
  num_iter = 200L,
  p_bounds = c(1e-05, 1)
)
}
\arguments{
\item{corr}{An \link[Rfmalloc:fmalloc_ld]{Rfmalloc::fmalloc_ld} banded LD (correlation) matrix, e.g.
from \code{\link[=statgen_snp_cor]{statgen_snp_cor()}}. Its dimension must equal \code{nrow(df_beta)}.}

\item{df_beta}{A data frame of GWAS summary statistics with columns \code{beta}
(marginal effect), \code{beta_se} (its standard error, all \verb{> 0}) and \code{n_eff}
(effective sample size), one row per variant in \code{corr}'s order (bigsnpr's
convention).}

\item{h2}{Heritability (a positive scalar) captured by the variants.}

\item{p}{Proportion of causal variants for \code{statgen_ldpred2()} (in \verb{(0, 1]}).}

\item{sparse}{Whether to seek a sparse solution (variants with posterior
causal probability below \code{p} are set exactly to \code{0}). Default \code{FALSE}.}

\item{burn_in}{Number of burn-in Gibbs iterations. Default \code{50}
(\code{500} for auto).}

\item{num_iter}{Number of iterations after burn-in whose posterior draws are
averaged. Default \code{100} (\code{200} for auto).}

\item{h2_init, p_init}{Initial heritability and polygenicity for the auto
sampler.}

\item{p_bounds}{Length-2 numeric bounds clamping the auto \code{p} estimate.
Default \code{c(1e-5, 1)}.}
}
\value{
\code{statgen_ldpred2_inf()} and \code{statgen_ldpred2()} return a numeric
vector of posterior-mean effect sizes on the allele scale, one per variant
(\code{NA} for every variant if the Gibbs sampler diverged).
\code{statgen_ldpred2_auto()} returns a list with \code{beta_est} (allele-scale
effects), \code{postp_est} (posterior causal probabilities), \code{p_est}, \code{h2_est}
(the averaged estimates) and the full \code{path_p_est} / \code{path_h2_est}.
}
\description{
LDpred2 (Prive, Arbel & Vilhjalmsson 2020,
\doi{10.1093/bioinformatics/btaa1029}) reweights GWAS summary statistics
using an LD matrix and a point-normal (spike-and-slab) prior on the effect
sizes. These functions are reimplemented clean-room from the paper and run
over an \link[Rfmalloc:fmalloc_ld]{Rfmalloc::fmalloc_ld} banded LD store (e.g. from
\code{\link[=statgen_snp_cor]{statgen_snp_cor()}}): they read the LD matrix one column's neighbour run at a
time and never touch the genotypes.
}
\details{
\code{statgen_ldpred2_inf()} is the deterministic infinitesimal model: every
variant is causal with effect \code{~ N(0, h2 / m)}, so the posterior mean solves
the ridge-like system \eqn{(C + \tfrac{m}{h2\,N} I)\,\beta = \hat\beta}
(\code{C} the LD matrix), here by conjugate gradient over the band. It matches
\code{bigsnpr::snp_ldpred2_inf()} to solver tolerance.

\code{statgen_ldpred2()} is the LDpred2-grid Gibbs sampler for a fixed
polygenicity \code{p} and heritability \code{h2}: with probability \code{p} a variant is
causal with effect \verb{~ N(0, h2 / (m p))}, else zero. It returns the
Rao-Blackwellized posterior-mean effects.

\code{statgen_ldpred2_auto()} is LDpred2-auto: the same Gibbs sampler but with \code{p}
and \code{h2} estimated from the data at each iteration (the \code{alpha = -1},
\verb{sigma2 = h2 / (m p)} variant), returning the estimates alongside the
effects.
}
\examples{
rt <- Rfmalloc::open_fmalloc(tempfile(fileext = ".bin"), mode = "scratch",
                             size_gb = 0.1)
set.seed(1)
m <- 20L
## a simple tridiagonal LD matrix
i <- j <- x <- numeric(0)
for (col in seq_len(m)) {
    rows <- max(1L, col - 1L):min(m, col + 1L)
    i <- c(i, rows); j <- c(j, rep(col, length(rows)))
    x <- c(x, ifelse(rows == col, 1, 0.3))
}
corr <- Rfmalloc::fmalloc_ld(i, j, x, n_variants = m, bits = 16L, runtime = rt)
df_beta <- data.frame(beta = rnorm(m, sd = 0.02), beta_se = rep(0.01, m),
                      n_eff = rep(10000, m))
beta_inf <- statgen_ldpred2_inf(corr, df_beta, h2 = 0.1)
head(beta_inf)
Rfmalloc::cleanup_fmalloc(rt)
}
\seealso{
\code{\link[=statgen_snp_cor]{statgen_snp_cor()}}, \link[Rfmalloc:fmalloc_ld]{Rfmalloc::fmalloc_ld}
}
