% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/statgen_pca.R
\name{statgen_pca}
\alias{statgen_pca}
\title{Fast principal-component analysis by PCAone randomized SVD}
\usage{
statgen_pca(
  X,
  k,
  p = 7L,
  s = 10L,
  center = FALSE,
  scale = FALSE,
  tol = 1e-04,
  seed = 112L
)
}
\arguments{
\item{X}{A numeric matrix (samples x variants), or anything
\code{\link[base:matrix]{base::as.matrix()}} turns into one.}

\item{k}{Number of principal components to return (a positive integer,
\code{k >= 1}).}

\item{p}{Number of power iterations (default \code{7}). More iterations sharpen
the top-\code{k} subspace at the cost of extra passes over \code{X}.}

\item{s}{Oversampling size (default \code{10}): the random test matrix has
\code{k + s} columns, and \code{k + s <= min(dim(X))} is required.}

\item{center, scale}{Logical, whether to centre / scale the columns of \code{X}
before decomposing (as \code{\link[base:scale]{base::scale()}} does). Defaults \code{FALSE} (a raw SVD
of \code{X}, matching \code{\link[base:svd]{base::svd()}}).}

\item{tol}{Subspace-convergence tolerance for the power-iteration loop
(default \code{1e-4}); the iteration stops early once successive estimates of
the top-\code{k} subspace agree to \code{tol}.}

\item{seed}{Integer seed for the Gaussian random test matrix (default
\code{112}, PCAone's own default), so runs are reproducible.}
}
\value{
A list with elements \code{d} (length-\code{k} singular values), \code{u}
(\code{nrow(X)} x \code{k} left singular vectors) and \code{v} (\code{ncol(X)} x \code{k} right
singular vectors).
}
\description{
Computes the top-\code{k} singular triplet of a matrix with the randomized SVD
of Li (2023, \doi{10.1101/2022.05.25.493261}), vendored verbatim from
'PCAone' (\verb{src/pcaone/}, GPL-3) and driven here over an in-memory matrix.
This is the "be the pointer, don't reimplement" path: the RSVD workhorse
(\code{RsvdOpData::computeUSV}) is PCAone's own block-wise power-iteration code,
not a reimplementation, so results track \code{PCAone}/\code{pcaone} bit-for-bit given
the same seed and parameters.
}
\details{
For a matrix \code{X} (samples x variants) the result satisfies
\code{X ~ u \%*\% diag(d) \%*\% t(v)}, matching the convention of \code{\link[base:svd]{base::svd()}}: \code{d}
are the top-\code{k} singular values, \code{u} (samples x \code{k}) the left singular
vectors, \code{v} (variants x \code{k}) the right singular vectors. With
\code{center}/\code{scale}, the decomposition is of the centred/scaled matrix, so \code{d}
relates to \code{\link[stats:prcomp]{stats::prcomp()}}'s \code{sdev} by \code{d = sdev * sqrt(nrow(X) - 1)} and
\code{v} is its rotation.

Randomized SVD is exact only in the limit; accuracy on the top \code{k}
components improves with the number of power iterations \code{p} and the
oversampling \code{s}, and is excellent when the spectrum decays (as genotype
data's does). \code{k + s} must not exceed \code{min(nrow(X), ncol(X))}.
}
\examples{
set.seed(1)
## a matrix with a decaying spectrum, where RSVD is near-exact
U <- qr.Q(qr(matrix(rnorm(200 * 10), 200)))
V <- qr.Q(qr(matrix(rnorm(60 * 10), 60)))
X <- U \%*\% diag(5 * 0.6^(0:9)) \%*\% t(V)
pc <- statgen_pca(X, k = 3)
pc$d
svd(X)$d[1:3]
}
\seealso{
\code{\link[base:svd]{base::svd()}}, \code{\link[stats:prcomp]{stats::prcomp()}}
}
