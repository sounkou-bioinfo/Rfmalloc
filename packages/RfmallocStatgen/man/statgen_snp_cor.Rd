% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/statgen_snp_cor.R
\name{statgen_snp_cor}
\alias{statgen_snp_cor}
\title{Windowed LD (correlation) matrix from a genotype tensor}
\usage{
statgen_snp_cor(genotypes, size, thr_r2 = 0, infos_pos = NULL, bits = 8L)
}
\arguments{
\item{genotypes}{A \code{"bed"} or \code{"dosage"} \link[Rfmalloc:fmalloc_tensor]{Rfmalloc::fmalloc_tensor}
(samples x variants), the variants position-sorted.}

\item{size}{Window half-width. With \code{infos_pos = NULL} (the default) this is
a window in \strong{number of variants} each side (variant \code{j} is correlated
with variants \verb{j - size .. j + size}). When \code{infos_pos} is supplied, it is
a window in \strong{kilobases} of physical distance (bigsnpr's convention: the
window in position units is \code{size * 1000}).}

\item{thr_r2}{Squared-correlation threshold: correlations with \code{r^2 < thr_r2}
are set to \code{0} within the band. Default \code{0} (keep every in-window
correlation).}

\item{infos_pos}{\code{NULL} (window by variant index) or a numeric vector of
physical positions, one per variant, \strong{sorted} (window by genomic
distance).}

\item{bits}{Quantization width for the stored correlations, \code{8} (int8, the
default; resolution \code{~1/127}) or \code{16} (int16; resolution \code{~3e-5}).}
}
\value{
An \link[Rfmalloc:fmalloc_ld]{Rfmalloc::fmalloc_ld} banded LD matrix (\code{ncol(genotypes)} x
\code{ncol(genotypes)}).
}
\description{
Computes the (Pearson) correlations between nearby variants of a \code{bed} or
\code{dosage} \link[Rfmalloc:fmalloc_tensor]{Rfmalloc::fmalloc_tensor} and stores them as a compressed, banded
\link[Rfmalloc:fmalloc_ld]{Rfmalloc::fmalloc_ld} LD matrix. This is the fused form
\code{bigsnpr::snp_cor()} uses (Prive et al. 2018,
\doi{10.1093/bioinformatics/bty185}): the genotype tensor is streamed one
variant column at a time through \link[Rfmalloc:fmalloc_tensor]{Rfmalloc::fmalloc_tensor}'s decode (never
materializing the dense samples x variants matrix), each column is
mean-imputed, centred and normalized to unit L2 norm, and the correlation of
two nearby variants is then the dot product of their unit columns. Only a
sliding window of unit columns (bounded by the window width, not the variant
count) is held resident.
}
\details{
Because the variants are assumed position-sorted, each variant's in-window
neighbours are a contiguous index range, so the full symmetric band is kept
per column with no explicit neighbour indices (see \link[Rfmalloc:fmalloc_ld]{Rfmalloc::fmalloc_ld}).
The diagonal is \code{1}. The result is storage-agnostic: read it with
\code{\link[Rfmalloc:ld_col]{Rfmalloc::ld_col()}} / \code{\link[Rfmalloc:ld_pair]{Rfmalloc::ld_pair()}}, or hand it to LDpred2.

Missing genotype calls are mean-imputed per variant before the correlation
(matching \code{\link[Rfmalloc:fmalloc_bed_standardize]{Rfmalloc::fmalloc_bed_standardize()}}). On complete data this is
the same estimator as \code{bigsnpr::snp_cor()}; with missingness the two differ
(bigsnpr deletes pairwise-incomplete samples instead of imputing).
}
\examples{
rt <- Rfmalloc::open_fmalloc(tempfile(fileext = ".bin"), mode = "scratch",
                             size_gb = 0.1)
set.seed(1)
n <- 100L; m <- 20L
g <- matrix(sample(0:2, n * m, replace = TRUE), nrow = n, ncol = m)
storage.mode(g) <- "integer"
tn <- Rfmalloc::fmalloc_bed(g, runtime = rt)
corr <- statgen_snp_cor(tn, size = 3)
Rfmalloc::ld_pair(corr, 1, 2)
Rfmalloc::cleanup_fmalloc(rt)
}
\seealso{
\link[Rfmalloc:fmalloc_ld]{Rfmalloc::fmalloc_ld}, \code{\link[Rfmalloc:ld_col]{Rfmalloc::ld_col()}},
\code{\link[Rfmalloc:ld_pair]{Rfmalloc::ld_pair()}}, \code{\link[=statgen_ldpred2_inf]{statgen_ldpred2_inf()}}
}
