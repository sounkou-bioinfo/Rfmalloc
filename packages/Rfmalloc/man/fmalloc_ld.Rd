% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fmalloc_ld.R
\name{fmalloc_ld}
\alias{fmalloc_ld}
\alias{dim.fmalloc_ld}
\alias{print.fmalloc_ld}
\title{Banded LD (correlation) matrix as a compressed fmalloc store}
\usage{
fmalloc_ld(i, j, x, n_variants, bits = 8L, window = 0L, runtime = NULL)

\method{dim}{fmalloc_ld}(x)

\method{print}{fmalloc_ld}(x, ...)
}
\arguments{
\item{i, j}{Integer vectors of 1-based row/column indices of the stored
correlations (COO triplets). The band of column \code{j} is taken as the
contiguous range spanning every \code{i} seen for that \code{j} (always including
the diagonal); interior gaps are stored as \code{0}.}

\item{x}{An \code{fmalloc_ld} object.}

\item{n_variants}{Number of variants (the matrix is \code{n_variants} x
\code{n_variants}).}

\item{bits}{Quantization width, \code{8} (int8, the default) or \code{16} (int16).}

\item{window}{Optional integer recording the build window (informational,
stored in the header); \code{0} if unknown.}

\item{runtime}{Runtime handle from \code{\link[=open_fmalloc]{open_fmalloc()}}; defaults to the runtime
established by \code{\link[=init_fmalloc]{init_fmalloc()}}.}

\item{...}{Unused.}
}
\value{
An \code{fmalloc_ld} object (a compressed, mmap-backed banded LD matrix).
}
\description{
Stores a banded symmetric linkage-disequilibrium (Pearson correlation)
matrix in fmalloc-backed, memory-mapped storage as quantized integers. This
is a SIBLING interface to the matmul \link{fmalloc_tensor} codec ABI, not an
instance of it (like \code{\link[=fmalloc_haplotypes]{fmalloc_haplotypes()}}): an LD matrix is read one
column's neighbours at a time by a Gibbs sampler or a ridge solve
(LDpred2), never multiplied as a decoded dense \verb{p x p} double matrix, so
the store is a typed accessor with its own read API and never participates
in \code{\%*\%}.
}
\details{
The variants are assumed position-sorted, so each column \code{j}'s in-window
neighbours form a contiguous index range \verb{[lo_j, hi_j]}. The store keeps the
full symmetric band per column (both \code{r[j, k]} and \code{r[k, j]}), the diagonal
is \code{1}, and no explicit neighbour indices are stored - the row of a column's
\code{t}-th stored value is \code{lo_j + t}. A per-column offset table gives O(1)
random access to any column's neighbour run and a cache-friendly banded
matvec.

Correlations are quantized to \code{bits}-wide integers: \code{r} in \verb{[-1, 1]} becomes
\code{round(r * S)} clamped to \verb{[-S, S]}, decoded back as \code{q / S}, with \code{S = 127}
for \code{bits = 8} (int8, resolution \code{~1/127}) or \code{S = 32767} for \code{bits = 16}
(int16, resolution \code{~3e-5}).

\code{fmalloc_ld()} builds a store from \verb{(i, j, x)} correlation triplets;
RfmallocStatgen's \code{statgen_snp_cor()} builds one directly from a genotype
tensor. Read it with \code{\link[=ld_ncol]{ld_ncol()}}, \code{\link[=ld_pair]{ld_pair()}} and \code{\link[=ld_col]{ld_col()}}.
}
\examples{
rt <- open_fmalloc(tempfile(), size_gb = 0.1)
## a 4-variant tridiagonal correlation matrix
i <- c(1, 2, 1, 2, 3, 2, 3, 4, 3, 4)
j <- c(1, 1, 2, 2, 2, 3, 3, 3, 4, 4)
x <- c(1, 0.5, 0.5, 1, 0.3, 0.3, 1, -0.2, -0.2, 1)
corr <- fmalloc_ld(i, j, x, n_variants = 4, runtime = rt)
ld_pair(corr, 1, 2)
ld_col(corr, 2)
cleanup_fmalloc(rt)
}
\seealso{
\code{\link[=ld_ncol]{ld_ncol()}}, \code{\link[=ld_pair]{ld_pair()}}, \code{\link[=ld_col]{ld_col()}}
}
