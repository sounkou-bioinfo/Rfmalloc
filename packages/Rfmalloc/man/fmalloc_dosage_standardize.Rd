% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fmalloc_dosage.R
\name{fmalloc_dosage_standardize}
\alias{fmalloc_dosage_standardize}
\title{Bake per-variant standardization into a dosage tensor}
\usage{
fmalloc_dosage_standardize(x, scale = c("sd", "binomial"), runtime = NULL)
}
\arguments{
\item{x}{A \code{"dosage"} \link{fmalloc_tensor} from \code{\link[=fmalloc_dosage]{fmalloc_dosage()}} (raw, not
already standardized).}

\item{scale}{One of \code{"sd"} (default; the sample standard deviation of the
mean-imputed column, matching \code{\link[=scale]{scale()}}) or \code{"binomial"}
(\verb{sqrt(2 p (1 - p))}, \code{p = mean/2}).}

\item{runtime}{Runtime handle from \code{\link[=open_fmalloc]{open_fmalloc()}}; defaults to the runtime
established by \code{\link[=init_fmalloc]{init_fmalloc()}}.}
}
\value{
A \code{"dosage"} \code{fmalloc_tensor} whose decode is standardized.
Monomorphic variants (zero variance) decode to \code{0}.
}
\description{
Computes each variant's mean and standard deviation in one streaming pass and
stores them in the tensor, so that every subsequent decode returns
standardized, mean-imputed values: missing dosages decode to the variant mean,
hence to \code{0} after centering. Products against the standardized tensor are
therefore centered-and-scaled with no dosage ever materialized as a double and
no second pass, which is what a dosage-based PCA or GRM needs. The mirror of
\code{\link[=fmalloc_bed_standardize]{fmalloc_bed_standardize()}} for continuous dosages.
}
\examples{
rt <- open_fmalloc(tempfile(), size_gb = 0.1)
d <- matrix(c(0, 0.3, 1.7, 2, 1.1, 0.9), nrow = 3, ncol = 2)
tn <- fmalloc_dosage_standardize(fmalloc_dosage(d, runtime = rt), runtime = rt)
round(fmalloc_tensor_materialize(tn), 3)
cleanup_fmalloc(rt)
}
\seealso{
\code{\link[=fmalloc_dosage]{fmalloc_dosage()}}, \code{\link[=fmalloc_bed_standardize]{fmalloc_bed_standardize()}}
}
