% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fmalloc_dosage.R
\name{fmalloc_dosage}
\alias{fmalloc_dosage}
\title{Fractional genotype dosages as a 1-byte fmalloc tensor}
\usage{
fmalloc_dosage(x, runtime = NULL)
}
\arguments{
\item{x}{A numeric matrix of dosages in \verb{[0, 2]}, with \code{NA} for missing.}

\item{runtime}{Runtime handle from \code{\link[=open_fmalloc]{open_fmalloc()}}; defaults to the runtime
established by \code{\link[=init_fmalloc]{init_fmalloc()}}.}
}
\value{
An \code{fmalloc_tensor} of dtype \code{"dosage"} with \code{dim(x)}.
}
\description{
Encodes a numeric matrix of dosages (values in \verb{[0, 2]}, \code{NA} allowed) into
fixed-point single-byte storage as an \link{fmalloc_tensor} of codec \code{"dosage"},
the continuous sibling of \code{\link[=fmalloc_bed]{fmalloc_bed()}}. Samples are rows, variants are
columns. A dosage \code{d} is stored as \code{round(d * 127)} (\verb{0..254}), with \code{255}
reserved for missing, so the resolution is \code{2 / 254}. This is lossy by
design: it is a storage codec, eight times tighter than the doubles it decodes
to, and it is the target a PLINK 2 \code{.pgen} dosage import re-encodes into.
}
\details{
Products against the tensor decode bounded column panels and contract them
with BLAS, so dosages are never materialized as doubles.
}
\examples{
rt <- open_fmalloc(tempfile(), size_gb = 0.1)
d <- matrix(c(0, 0.3, 1.7, NA, 2, 0.9), nrow = 3, ncol = 2)
tn <- fmalloc_dosage(d, runtime = rt)
round(fmalloc_tensor_materialize(tn), 2)
cleanup_fmalloc(rt)
}
\seealso{
\code{\link[=fmalloc_bed]{fmalloc_bed()}}, \code{\link[=fmalloc_dosage_standardize]{fmalloc_dosage_standardize()}}
}
