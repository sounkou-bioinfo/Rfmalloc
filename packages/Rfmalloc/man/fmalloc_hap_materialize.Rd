% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fmalloc_hap.R
\name{fmalloc_hap_materialize}
\alias{fmalloc_hap_materialize}
\title{Adapt a bit-packed haplotype store to a 0/1 matrix}
\usage{
fmalloc_hap_materialize(x, runtime = NULL)
}
\arguments{
\item{x}{An \code{fmalloc_haplotypes} object from \code{\link[=fmalloc_haplotypes]{fmalloc_haplotypes()}}.}

\item{runtime}{Runtime handle from \code{\link[=open_fmalloc]{open_fmalloc()}}; defaults to the
runtime established by \code{\link[=init_fmalloc]{init_fmalloc()}}.}
}
\value{
An fmalloc-backed integer matrix of \code{0L}/\code{1L} calls with \code{dim(x)}.
}
\description{
Decodes an \code{\link[=fmalloc_haplotypes]{fmalloc_haplotypes()}} store back into an \verb{L x N} matrix of
\code{0L}/\code{1L} integer calls, variants in rows and haplotypes in columns. The
result is itself fmalloc-backed (file-mapped storage, not an R-heap copy):
this is the same "decode into typed storage, not into a plain R object"
discipline \code{\link[=fmalloc_tensor_materialize]{fmalloc_tensor_materialize()}} uses for the matmul codecs.
}
\details{
This adapter exists for consumers that require a conventional R matrix.
Native HMM consumers should instead resolve \code{Rfmalloc_haplotypes_data()}
from the installed C header and borrow the aligned locus rows directly.
\code{kalis::CacheHaplotypes()} can consume the adapted matrix, but a kalis
packed-buffer method can use the direct view without either expansion or
repacking.
}
\examples{
rt <- open_fmalloc(tempfile(), size_gb = 0.1)
h <- matrix(c(0L, 1L, 1L, 0L, 1L, 0L), nrow = 3, ncol = 2)
hap <- fmalloc_haplotypes(h, runtime = rt)
identical(fmalloc_hap_materialize(hap, runtime = rt)[], h)
cleanup_fmalloc(rt)
}
\seealso{
\code{\link[=fmalloc_haplotypes]{fmalloc_haplotypes()}}
}
