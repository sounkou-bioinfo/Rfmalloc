% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fmalloc_ooc.R
\name{fmalloc_sync}
\alias{fmalloc_sync}
\title{Flush an fmalloc runtime's backing store to disk}
\usage{
fmalloc_sync(runtime = NULL, wait = TRUE)
}
\arguments{
\item{runtime}{Runtime handle from \code{\link[=open_fmalloc]{open_fmalloc()}}; defaults to the runtime
established by \code{\link[=init_fmalloc]{init_fmalloc()}}.}

\item{wait}{If \code{TRUE} (default), block until the flush completes
(\code{MS_SYNC}); if \code{FALSE}, schedule an asynchronous flush (\code{MS_ASYNC}).}
}
\value{
The number of bytes flushed, invisibly.
}
\description{
Writes to an fmalloc runtime (including in-place mutations via
\code{\link[=fmalloc_set]{fmalloc_set()}} and friends) land in the OS page cache of the \code{MAP_SHARED}
backing file. They survive a normal process exit, but until the kernel
writes dirty pages back — which it does asynchronously — a crash or power
loss can lose unsynced data, with no atomicity. \code{fmalloc_sync()} forces the
durability barrier with \code{msync()} (and \code{fsync()}), so persistent data is on
disk when it returns.
}
\details{
This matters only for \code{persistent} runtimes where durability across an
unclean shutdown is required; \code{scratch} runtimes are ephemeral by design.
On platforms without \code{msync} (e.g. the Windows/Rtools toolchain) this is a
no-op and returns 0, relying on OS writeback.
}
