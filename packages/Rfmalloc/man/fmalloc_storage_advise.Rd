% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fmalloc_tensor.R
\name{fmalloc_storage_advise}
\alias{fmalloc_storage_advise}
\title{Give the pager an access hint for typed storage}
\usage{
fmalloc_storage_advise(
  x,
  advice = c("sequential", "willneed", "dontneed"),
  offset = 0,
  nbytes = NULL
)
}
\arguments{
\item{x}{An \code{fmalloc_tensor} or its raw fmalloc payload.}

\item{advice}{One of \code{"sequential"}, \code{"willneed"}, or \code{"dontneed"}.}

\item{offset}{Byte offset into the payload.}

\item{nbytes}{Number of bytes to advise. \code{NULL} covers the rest of the
payload.}
}
\value{
\code{x}, invisibly.
}
\description{
Applies a best-effort access hint to an fmalloc tensor payload or borrowed
storage view. \code{"sequential"} asks the pager to read ahead, \code{"willneed"}
asks it to prefetch, and \code{"dontneed"} releases fully covered pages after a
phase has consumed them. Unsupported platforms treat the hint as a no-op.
The function never changes the tensor bytes.
}
\details{
This is deliberately a storage primitive rather than an LLM policy. A graph
scheduler, HMM, or out-of-core matrix algorithm can express its own access
phases over the same mapped spans.
}
