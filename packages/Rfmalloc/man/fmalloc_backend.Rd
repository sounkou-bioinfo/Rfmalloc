% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fmalloc_backend.R
\name{fmalloc_backend}
\alias{fmalloc_backend}
\alias{fmalloc_matmul_backend}
\alias{fmalloc_matmul_backends}
\title{Pluggable matrix-multiply backend}
\usage{
fmalloc_matmul_backend(name = NULL)

fmalloc_matmul_backends()
}
\arguments{
\item{name}{Backend name to select. \code{"blas"} (or \code{NULL}/\code{""}) selects the
default BLAS path.}
}
\value{
\code{fmalloc_matmul_backend()} returns the active backend name;
\code{fmalloc_matmul_backends()} returns the registered backend names (BLAS is
always available and not listed).
}
\description{
Rfmalloc's matrix-product kernels (\code{\%*\%}, \code{crossprod()}, \code{tcrossprod()},
the out-of-core and typed-tensor products) dispatch their \code{dgemm} calls
through a selectable backend. By default this is R's BLAS; downstream
packages can register an alternative (for example a GPU cuBLAS kernel or an
out-of-core-aware GEMM) through the \code{Rfmalloc_register_matmul_backend}
C-callable and select it here. Selection is Rfmalloc-scoped: base R's \code{\%*\%}
is unaffected.
}
\details{
A registered backend may decline a given call (returning non-zero), in which
case Rfmalloc falls back to R's BLAS for that product.
}
\examples{
fmalloc_matmul_backend()      # "blas" by default
fmalloc_matmul_backends()     # names registered by other packages

}
