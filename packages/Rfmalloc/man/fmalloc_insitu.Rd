% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fmalloc_insitu.R
\name{fmalloc_insitu}
\alias{fmalloc_insitu}
\alias{fmalloc_set}
\alias{fmalloc_fill}
\alias{fmalloc_add}
\alias{fmalloc_sub}
\alias{fmalloc_mul}
\alias{fmalloc_div}
\title{In-place (by-reference) mutation of fmalloc vectors}
\usage{
fmalloc_set(x, i, value)

fmalloc_fill(x, value)

fmalloc_add(x, y)

fmalloc_sub(x, y)

fmalloc_mul(x, y)

fmalloc_div(x, y)
}
\arguments{
\item{x}{An fmalloc-backed atomic vector (or matrix/array).}

\item{i}{Positive integer (1-based) linear indices to assign.}

\item{value}{For \code{fmalloc_set()}, a vector of length 1 (recycled) or
\code{length(i)}. For \code{fmalloc_fill()}, a single scalar.}

\item{y}{For the arithmetic ops, a numeric scalar (recycled) or a vector of
\code{length(x)}. \code{NA}/\code{NaN}/\code{Inf} follow IEEE double arithmetic (base R
semantics).}
}
\value{
\code{x}, invisibly, mutated in place.
}
\description{
Modify an fmalloc-backed atomic vector \emph{in place}, writing straight through
the backing store and deliberately bypassing R's copy-on-modify.
\code{fmalloc_set()}/\code{fmalloc_fill()} are the by-reference analogue of
\code{x[i] <- value} / \code{x[] <- value}; \code{fmalloc_add()}/\code{fmalloc_sub()}/
\code{fmalloc_mul()}/\code{fmalloc_div()} compute \verb{x <- x op y} in place (the
accumulate-into-\code{x} pattern iterative algorithms need), for numeric vectors.
}
\details{
On an \emph{unshared} fmalloc vector, an ordinary \code{x[i] <- value} already writes
in place through the ALTREP data pointer — no copy — because the file-backed
storage is exposed directly and this package controls duplication. The copy
that hurts happens when the vector is \emph{shared} (\code{y <- x}): R then duplicates
the whole payload to preserve value semantics before modifying, which is
catastrophic for a larger-than-RAM or persistent vector. These functions
mutate by reference regardless of sharing — they never copy, updating the
durable store directly and returning the same object invisibly.
}
\section{Aliasing (read this)}{

Because there is no copy, all bindings to the same fmalloc vector observe the
change. After \verb{y <- x; fmalloc_set(x, 1, 5)}, \code{y[1]} is also \code{5} — \code{x} and
\code{y} name the same backing store, whereas ordinary \code{x[1] <- 5} would copy \code{x}
(leaving \code{y} untouched). This breaks R's usual value semantics \emph{by design};
for a persistent runtime it is a feature (the durable data is updated). For
this reason mutation is only ever done through these explicitly-named
functions, never a silent \verb{[<-} method.

Supported for fixed-width atomic vectors (logical, integer, numeric,
complex, raw). Indices are 1-based linear positions (column-major for
matrices/arrays).
}

\examples{
\dontrun{
rt <- open_fmalloc(tempfile(fileext = ".bin"))
x <- create_fmalloc_vector("numeric", 5, runtime = rt)
fmalloc_fill(x, 0)            # x[] <- 0, no copy
fmalloc_set(x, c(1, 3), 9)    # x[c(1,3)] <- 9, no copy
cleanup_fmalloc(rt)
}

}
