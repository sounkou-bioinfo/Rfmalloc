% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gguf_write.R
\name{gguf_write_tensors}
\alias{gguf_write_tensors}
\title{Write a Minimal GGUF File}
\usage{
gguf_write_tensors(path, tensors, metadata = list())
}
\arguments{
\item{path}{Character string giving the output file path.}

\item{tensors}{A non-empty named list of numeric vectors, matrices, or
arrays. Names become tensor names and must be unique and non-empty.}

\item{metadata}{A named list of metadata key-value pairs to write. Values
may be non-missing character vectors or single non-missing numeric
values. Character vectors of length other than one are written as GGUF
string arrays; numeric values are written as 64-bit floats (\code{FLOAT64}).
Defaults to \code{list()} (no metadata).}
}
\value{
\code{path}, invisibly.
}
\description{
Writes a named list of numeric vectors/matrices/arrays to a GGUF file as
32-bit floating point (\code{f32}) tensors, with optional simple metadata. This
is a minimal writer, primarily meant to build small GGUF fixtures for this
package's own round-trip tests without shipping binary test fixtures, but
it is exported since it is also useful on its own for producing test
fixtures for other GGUF consumers.
}
\details{
Tensor dimensions are taken from each object's \code{dim()} (or its \code{length()}
for a plain vector, written as a 1-dimensional tensor) and stored in GGUF
\code{dim[0]}-fastest-varying order directly from R's column-major storage, so
\code{gguf_tensor()}/\code{gguf_import()} read back exactly the matrix/array you
wrote (see \code{inst/tinytest/test_gguf_roundtrip.R}).

Existing files at \code{path} are silently overwritten.
}
\examples{
tmp <- tempfile(fileext = ".gguf")
gguf_write_tensors(tmp,
    tensors = list(weight = matrix(1:6 + 0.5, nrow = 2)),
    metadata = list(name = "example-model", version = 1)
)
gguf_tensors(tmp)
unlink(tmp)

}
