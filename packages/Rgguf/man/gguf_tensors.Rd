% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gguf_tensors.R
\name{gguf_tensors}
\alias{gguf_tensors}
\title{List GGUF Tensors}
\usage{
gguf_tensors(x)
}
\arguments{
\item{x}{Either a path to a GGUF file, or a \code{"gguf_ctx"} object returned by
\code{\link[=gguf_open]{gguf_open()}}.}
}
\value{
A data frame with one row per tensor and columns:
\describe{
\item{name}{Tensor name.}
\item{type}{GGUF tensor type name, e.g. \code{"f32"}, \code{"q4_k"}.}
\item{n_dims}{Number of dimensions.}
\item{dims}{A list column; \code{dims[[i]]} is an integer vector of tensor
\code{i}'s dimensions, GGUF-order (\code{dim[0]} first, the fastest-varying/
contiguous dimension, which \code{\link[=gguf_tensor]{gguf_tensor()}} maps to R's first,
column-major dimension).}
\item{n_elements}{Total number of scalar elements.}
\item{nbytes}{Size of the tensor's raw (possibly quantized) on-disk
representation, in bytes.}
\item{offset}{Byte offset of the tensor data from the start of the
file.}
}
}
\description{
Lists the tensor directory of a GGUF file as a data frame, without reading
or dequantizing any tensor payload.
}
\examples{
tmp <- tempfile(fileext = ".gguf")
gguf_write_tensors(tmp, list(w = matrix(1:6 + 0.5, nrow = 2), b = 1:3 + 0.5))
gguf_tensors(tmp)
unlink(tmp)

}
