% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fmalloc.R
\name{create_fmalloc_matrix}
\alias{create_fmalloc_matrix}
\title{Create Matrix Using fmalloc}
\usage{
create_fmalloc_matrix(
  type = "integer",
  nrow,
  ncol,
  dimnames = NULL,
  runtime = NULL
)
}
\arguments{
\item{type}{Character string specifying the vector type. Supported values are
the same as for \code{\link[=create_fmalloc_vector]{create_fmalloc_vector()}}.}

\item{nrow}{Integer number of rows.}

\item{ncol}{Integer number of columns.}

\item{dimnames}{Optional \code{dimnames} list for the matrix.}

\item{runtime}{Optional runtime handle returned by \code{\link[=open_fmalloc]{open_fmalloc()}}. If not
supplied, the default runtime established by \code{\link[=init_fmalloc]{init_fmalloc()}} is used.}
}
\value{
An fmalloc-backed ALTREP matrix object.
}
\description{
Creates an fmalloc-backed ALTREP matrix in a single step by allocating vector
storage and installing matrix dimensions (and optional dimnames).
}
\examples{
\dontrun{
rt <- open_fmalloc(tempfile(fileext = ".bin"))
m <- create_fmalloc_matrix("integer", nrow = 2, ncol = 3, runtime = rt)
cleanup_fmalloc(rt)
}

}
