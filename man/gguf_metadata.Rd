% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gguf_metadata.R
\name{gguf_metadata}
\alias{gguf_metadata}
\title{Read GGUF Metadata}
\usage{
gguf_metadata(x)
}
\arguments{
\item{x}{Either a path to a GGUF file, or a \code{"gguf_ctx"} object returned by
\code{\link[=gguf_open]{gguf_open()}}.}
}
\value{
A named list of metadata values, in file order.
}
\description{
Reads every metadata key-value pair from a GGUF file into a named R list.
}
\details{
Scalar values are converted to the closest native R type (\code{integer} for
8/16/32-bit signed and unsigned-but-int-safe integers, \code{numeric} for
32-bit unsigned and 64-bit integers/floats, \code{logical} for booleans, and
\code{character} for strings). Arrays of a supported scalar type become an R
vector of that type. A metadata value of a type this package does not
represent (there are none in the current GGUF spec, but the check is
defensive) is returned as \code{NULL} while keeping its name, rather than
raising an error.
}
\examples{
tmp <- tempfile(fileext = ".gguf")
gguf_write_tensors(tmp, list(w = matrix(1:6 + 0.5, nrow = 2)),
    metadata = list(name = "example", version = 1))
gguf_metadata(tmp)
unlink(tmp)

}
