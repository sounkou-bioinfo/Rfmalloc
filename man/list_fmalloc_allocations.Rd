% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fmalloc.R
\name{list_fmalloc_allocations}
\alias{list_fmalloc_allocations}
\title{List Persistent fmalloc Allocations}
\usage{
list_fmalloc_allocations(runtime = NULL)
}
\arguments{
\item{runtime}{Optional runtime handle returned by \code{\link[=open_fmalloc]{open_fmalloc()}}. If not
supplied, the default runtime established by \code{\link[=init_fmalloc]{init_fmalloc()}} is used.}
}
\value{
A data frame with one row per catalog record and columns describing
the catalog record offset, generation, state, vector type, length, payload
offset, payload byte size, flags, and whether the record is recoverable by
reference serialization.
}
\description{
Returns the in-file allocation catalog for a persistent fmalloc runtime. The
catalog is stored in the backing file and records physical allocation metadata
used to validate serialized persistent references.
}
\examples{
\dontrun{
rt <- open_fmalloc(tempfile(fileext = ".bin"))
v <- create_fmalloc_vector("integer", 10, runtime = rt)
list_fmalloc_allocations(rt)
cleanup_fmalloc(rt)
}

}
