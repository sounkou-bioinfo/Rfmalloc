% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fmalloc.R
\name{create_fmalloc_vector}
\alias{create_fmalloc_vector}
\title{Create Vector Using fmalloc}
\usage{
create_fmalloc_vector(
  type = "integer",
  length,
  runtime = NULL,
  zero_initialize = TRUE
)
}
\arguments{
\item{type}{Character string specifying the vector type. Supported values are
\code{"logical"}, \code{"integer"}, \code{"numeric"}/\code{"double"}, \code{"raw"},
\code{"complex"}, \code{"character"}, and \code{"list"}. Fixed-width atomic types expose
a direct writable fmalloc \code{DATAPTR}; character vectors store string bytes in
fmalloc and materialize R \code{CHARSXP} values on demand; list vectors use
ALTREP element access with an R-visible reference sidecar for GC safety and
only accept \code{NULL} or fmalloc-backed vectors from the same runtime as
elements. Persistent list containers are serialized by nested reference
states when all elements are recoverable from the same runtime.}

\item{length}{Integer specifying the non-negative length of the vector to
create.}

\item{runtime}{Optional runtime handle returned by \code{\link[=open_fmalloc]{open_fmalloc()}}. If not
supplied, the default runtime established by \code{\link[=init_fmalloc]{init_fmalloc()}} is used.}

\item{zero_initialize}{Logical scalar. If TRUE (default), newly allocated payload
bytes are zero-initialized. Set FALSE to skip initialization for faster large
allocations when you will fully initialize values yourself.}
}
\value{
A vector of the specified type and length, allocated using fmalloc.
}
\description{
Creates an ALTREP vector using a file-backed fmalloc runtime. The returned
object is ALTREP from creation time. Fixed-width atomic payload bytes are
allocated directly with fmalloc, and ALTREP duplication and vector subsetting
keep copy-on-write copies fmalloc-backed without using R's non-API
\code{Rf_allocVector3()} path.
}
\examples{
\dontrun{
rt <- open_fmalloc(tempfile(fileext = ".bin"))
v <- create_fmalloc_vector("integer", 1000, runtime = rt)
cleanup_fmalloc(rt)
}

}
