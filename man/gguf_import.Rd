% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gguf_import.R
\name{gguf_import}
\alias{gguf_import}
\title{Import GGUF Tensors}
\usage{
gguf_import(path_or_ctx, tensors = NULL, runtime = NULL)
}
\arguments{
\item{path_or_ctx}{Either a path to a GGUF file, or a \code{"gguf_ctx"} object
returned by \code{\link[=gguf_open]{gguf_open()}}.}

\item{tensors}{Optional character vector of tensor names to import. If
\code{NULL} (the default), every tensor in the file is imported.}

\item{runtime}{Optional \code{Rfmalloc} runtime handle passed through to every
\code{\link[=gguf_tensor]{gguf_tensor()}} call, so all imported tensors share the same backing
file. If \code{NULL}, \code{Rfmalloc}'s own default-runtime resolution is used for
each tensor.}
}
\value{
A named list of \code{Rfmalloc}-backed matrices/arrays, one per
imported tensor, in the order requested (or file order, if \code{tensors} is
\code{NULL}).
}
\description{
Reads some or all tensors from a GGUF file into a named list of
\pkg{Rfmalloc}-backed matrices/arrays, dequantizing as needed. This is a
thin convenience wrapper around repeated \code{\link[=gguf_tensor]{gguf_tensor()}} calls that shares
a single open file handle across all of them.
}
\examples{
tmp <- tempfile(fileext = ".gguf")
gguf_write_tensors(tmp, list(
    w1 = matrix(as.double(1:12), nrow = 4, ncol = 3),
    w2 = matrix(as.double(1:6), nrow = 3, ncol = 2)
))
rt <- Rfmalloc::open_fmalloc(tempfile(fileext = ".bin"))
mats <- gguf_import(tmp, runtime = rt)
prod <- mats$w1 \%*\% mats$w2
Rfmalloc::is_fmalloc_vector(prod)
unlink(tmp)

}
